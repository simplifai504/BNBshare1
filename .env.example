# Copia este archivo a .env y rellena los valores.
# .env no se sube a git (está en .gitignore).

# Dirección de la wallet treasury (recibe el 100% del tax de los tokens).
TREASURY_ADDRESS=0x...

# Clave privada de la treasury (SOLO servidor; nunca en el cliente).
TREASURY_PRIVATE_KEY=...
